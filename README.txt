Here's what it can do:
* save similar data for multiple users
* create tasks with names
* give those tasks one or more labels
* put them as subtasks under a supertask
* give those tasks exactly one project
* sort tasks by date due within a project or all together
* create projects
* create labels